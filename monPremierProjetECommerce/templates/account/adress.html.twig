{% extends 'base.html.twig' %}

{% block title %}Mes adresses - Mokolo Market{% endblock %}

{% block content %}

<a href="{{path('account_adress_add')}}" class="btn btn-info btn-sm float-right">Ajoutez une adresse</a>


<h1>Mes Adresses</h1>
Bienvenue {{ app.user.firstname }} dans votre espace.<br/> Vous allez pouvoir ici gérer toutes vos adresses. <br/>
<a href="{{path('account')}}">Retour </a>
<hr>

{% if app.user.adresses|length == 0 %}
<p class= "text-center">
Vous n'avez pas encore ajouté d'adresse dans votre compte client. Pour en ajouter une, veuillez <a href="{{path('account_adress_add')}}">cliquer ici</a>
</p>

{% else %}

 <div class="row">
    
    {% for adresse in app.user.adresses %}
<div class="col-md-4">

    <div class="card" style="width: 18rem;"> 
  <div class="card-body">
    <h5 class="card-title">{{adresse.name}}</h5>
    <p class="card-text">
    {{adresse.adress}}, <br/>
    {{adresse.postal}} - {{adresse.city}} - {{adresse.country}}
    </p>
    <a href="{{ path('account_adress_edit', { 'id': adresse.id }) }}">Modifier</a> | <a href="{{ path('account_adress_delete', {'id' : adresse.id})}}">supprimer</a>
  </div>
</div>
      </div>  
    {% endfor %}
    </div>
{% endif %}

{% endblock %}
