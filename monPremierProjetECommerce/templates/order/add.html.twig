{% extends 'base.html.twig' %}

{% block title %}Paiement de ma commande - Mokolo Market{% endblock %}

{% block content %}
<h2>Mon récapitulatif</h2>
<p>Verifiez vos informations avant de payer votre commande</p>
<hr>
<div class="row">
    <div class="col-md-6">
      Mon adresse de livraison<br/>


      Mon transporteur

    
    </div>

    <div class="col-md-6">
            <div class="text-center">
                <b>Récap de ma commande</b><br/>
                <p>Retrouvez le récapitulatif de vos produits</p>
            </div>


        <div class="order-sumary">
            {% for key,product in cart %}

            <div class="row  {% if key > 0 %} mt-2 {% endif %} ">

            <div class="col-2 my-auto">
            <img src="/images/{{product.product.illustration}}" alt="{{product.product.name}}" height="75px">
            </div>

            <div class="col-8">
            {{product.product.name}}<br/>
            <small>{{product.product.subtitle}}</samll>
            
            </div>
            
            <div class="col-2 my-auto">
            x{{ product.quantity }}
            </div>
                </div>
            {% endfor %}
        
        </div>

        <a href="#" class="btn btn-success mt-3">Payer</a>
    </div>

</div>
{% endblock %}
